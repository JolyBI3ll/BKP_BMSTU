from setuptools import setup, find_packages

setup(
    name="taiga-contrib-drawio",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    zip_safe=False,
)